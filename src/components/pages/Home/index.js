import React from "react";
import modules from "components";
const Home = () => {
  const { Default } = modules;
  return (
    <Default>
      <h1>Home page</h1>
    </Default>
  );
};

export default Home;
